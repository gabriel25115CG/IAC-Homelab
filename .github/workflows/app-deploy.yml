name: Deploy apps

on:
  push:
    branches: [master]
    paths:
      - "app/**"

# contents:write pour committer le bump de tag, packages:write pour pousser sur GHCR.
# Tout se passe dans ce même repo (monorepo), donc GITHUB_TOKEN suffit — pas de PAT.
permissions:
  contents: write
  packages: write

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      apps: ${{ steps.detect.outputs.apps }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed apps
        id: detect
        run: |
          before="${{ github.event.before }}"
          if [ -z "$before" ] || ! git cat-file -e "$before" 2>/dev/null; then
            before=$(git rev-parse HEAD~1 2>/dev/null || echo "$(git hash-object -t tree /dev/null)")
          fi
          apps=$(git diff --name-only "$before" "${{ github.sha }}" -- app/ | cut -d/ -f2 | sort -u | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "apps=$apps" | tee -a "$GITHUB_OUTPUT"

  build-and-deploy:
    needs: detect
    if: needs.detect.outputs.apps != '[]'
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        app: ${{ fromJson(needs.detect.outputs.apps) }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute image tag
        id: tag
        run: echo "tag=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: app/${{ matrix.app }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ matrix.app }}:${{ steps.tag.outputs.tag }}
            ghcr.io/${{ github.repository_owner }}/${{ matrix.app }}:latest

      - name: Install kustomize
        run: |
          curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
          sudo mv kustomize /usr/local/bin/

      - name: Bump image tag in manifest
        working-directory: kubernetes/apps/${{ matrix.app }}
        run: kustomize edit set image ghcr.io/${{ github.repository_owner }}/${{ matrix.app }}=ghcr.io/${{ github.repository_owner }}/${{ matrix.app }}:${{ steps.tag.outputs.tag }}

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add kubernetes/apps/${{ matrix.app }}/kustomization.yaml
          git commit -m "chore(${{ matrix.app }}): bump image to ${{ steps.tag.outputs.tag }}"
          git pull --rebase origin master
          git push origin master
